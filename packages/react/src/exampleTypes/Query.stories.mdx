import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { PropsTable } from '@storybook/components'
import { Query } from '.'
import F from 'futil-js'
import TestTree from './stories/testTree'
import { ThemeSwitcher } from '../stories/themePicker'

<Meta title="ExampleTypes|Query" component={Query} />

<ThemeSwitcher theme="base" />

# Query

<Preview>
  <Story name="Query">
    <Query tree={TestTree()} path={['query']} />
  </Story>
</Preview>
